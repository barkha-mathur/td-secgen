## Roadmap (MVP)

- [ ] `td-secgen init` – scaffold config and basic files
- [ ] CI/CD detector (GitHub Actions first)
- [ ] `td-secgen analyze-ci` for GitHub Actions
- [ ] Threat model schema + a small built-in library
- [ ] `td-secgen generate` – emit tests / CI jobs from threats
- [ ] Basic HTML/Markdown report of coverage
<<EOF

